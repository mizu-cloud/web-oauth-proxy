{
  "name": "web-oauth-proxy",
  "version": "0.1.0",
  "description": "Host-based OIDC reverse proxy with web admin UI",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "http-proxy-middleware": "^3.0.3",
    "openid-client": "^5.7.1"
  },
  "devDependencies": {
    "supertest": "^7.1.0"
  }
}
